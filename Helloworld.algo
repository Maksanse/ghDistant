Algo HelloWorld {
  fonction principale {
   // Création de la fenêtre
Créer une fenêtre de titre HELLO_WORLD
Mettre la dimension à 500, 500 // Taille modifiée pour l'issue 1
Configurer la fenêtre sur 'exitOnClose'


    // Création du label de texte
    Créer un label avec du texte centré et contenant le texte HELLO_WORLD_TEXT
    Créer la couleur cyan
    Mettre la couleur Cyan sur le label

    // Finalisation
    Ajouter le label sur la fenetre
    Afficher la fenêtre
  }
}
